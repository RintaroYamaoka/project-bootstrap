# <タスクの 1 文要旨>

- **日付**: YYYY-MM-DD
- **プロジェクト**: <プロジェクト名 or ドメイン>
- **言語 / フレームワーク**: <例: TypeScript / Vitest>
- **所要時間**: <例: 約 30 分>

---

## 1. タスク

<依頼内容を 1〜3 文で>

---

## 2. /plan 出力

`/plan <タスク>` で得た計画書を貼る。

\`\`\`markdown
## タスク
...

## スコープ
...

## 成功条件
...

## 探索したコンテキスト
...

## ステップ
...

## 検証済みの前提
...

## 未検証 / リスク
...
\`\`\`

---

## 3. Red — failing テスト

`/red <ステップ>` で `test-writer` に依頼した内容と結果。

**書いたテスト**: `<ファイルパス:行番号>`

\`\`\`<lang>
// テストコードを貼る
\`\`\`

**実行結果**: fail (期待通り) / fail (別理由) / pass (問題)
**失敗メッセージ**: <要点>

---

## 4. Green — 最小実装

`/green <ターゲット>` で `implementer` に依頼した内容と結果。

**変更したファイル**: `<ファイルパス:行範囲>`

\`\`\`<lang>
// 実装コードを貼る
\`\`\`

**ターゲットテスト**: pass
**広めのテストスイート**: pass / fail (regression があれば一覧)

---

## 5. Refactor — 構造改善

`/refactor <観点>` で `refactorer` に依頼した内容と結果。

**適用したリファクタ**:
1. <変更内容> — テスト結果: pass
2. ...

**戻したリファクタ** (あれば):
- <変更内容> — 理由: ...

---

## 6. Close — 変更をクローズ

確認したこと:

- [ ] スコープが依頼と一致
- [ ] 投機的コードなし
- [ ] テストが pass
- [ ] アーキテクチャ / 依存方向が保たれている
- [ ] (バグ修正なら) 回帰テストが残っている

残課題:

- <あれば>

---

## 7. 振り返り

**うまくいった点**:

- ...

**困った / 想定通りに行かなかった点**:

- ...

**原則 / プリミティブへの気付き**:

- (例: 「`/plan` の出力に X が抜けがち」「`test-writer` が Y のとき過剰に mock を作る」など、SKILL.md やプリミティブの改善材料)
