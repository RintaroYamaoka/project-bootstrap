<!--
これは新規プロジェクトに配置する CLAUDE.md の雛形です。
プロジェクトの root にコピーし、各セクションの `<!-- TODO -->` を埋めて使ってください。

このファイルにはプロジェクト固有の情報だけを書きます。
不変の原則 (SOLID / KISS / YAGNI / TDD など) は project-bootstrap プラグインが提供するため、ここに書き写しません。
プラグインの原則とプロジェクト固有の事情が矛盾する場合のみ、このファイルの記述で上書きします。
-->

# CLAUDE.md

## プロジェクト概要

<!-- TODO: このプロジェクトが何をするものか、1〜3 文で記述する -->

## ガイドライン

このプロジェクトは **`project-bootstrap`** プラグインの原則に従う。
コーディング作業を始める前に、必ず `project-bootstrap` skill を参照すること。

プラグインが提供する原則の要点:

- **Code is Truth** — 振る舞い仕様はテストで表現する。中間ドキュメント仕様を持たない
- **TDD 中心** — Red → Green → Refactor を作業の軸にする
- **古典原則** — SOLID / KISS / YAGNI / DRY (Rule of Three) / Fail-fast / Root-cause
- **環境隔離** — プロジェクトの import 依存はグローバルに置かず、`.venv` / `node_modules` / `Cargo.toml` 等で隔離する
- **AI 協働ルール** — 実装より先にテスト / 前提検証を強制 / Subagent でフェーズ分離 / Plan → Execute の二段構え

詳細は `project-bootstrap` プラグインの SKILL.md を参照する。

**矛盾が生じた場合**: プロジェクト固有の事情がプラグインの原則と矛盾するときだけ、このファイルの記述を優先する。それ以外は常にプラグインの原則に従う。

## 技術スタック

<!-- TODO: 主な言語・フレームワーク・主要ライブラリを箇条書きで -->

- 言語:
- フレームワーク:
- データベース:
- テストフレームワーク:
- リンター / フォーマッター:

## 開発コマンド

<!-- TODO: よく使うコマンドを記述する。AI がこのプロジェクトで作業するときの「実行手段」になる。 -->

```bash
# 依存インストール


# テスト実行 (全件)


# テスト実行 (単一ファイル / 単一ケース)


# リント


# 型チェック


# ビルド


# 開発サーバー起動

```

## ディレクトリマップ

<!-- TODO: 主要ディレクトリと役割を記述する。AI がコードベースを理解するための地図。 -->

```
.
├── src/          <!-- 説明 -->
├── tests/        <!-- 説明 -->
└── ...
```

## アーキテクチャ概略

<!-- TODO: 採用しているアーキテクチャパターン (レイヤー / ヘキサゴナル / モノリス / モジュラーなど) と、依存方向の規則を記述する。プラグインの「アーキテクチャ指針」を、このプロジェクトの実態に翻訳する。 -->

## プロジェクト固有の規約

<!-- TODO: このプロジェクトでだけ適用する規約があれば記述する。例: 命名規則、ファイル分割の方針、特定パターンの禁止、命名の例外。 -->

## 既知の地雷

<!-- TODO: 触るときに注意が必要なファイル / 領域 / 操作。過去に踏んだ地雷を記録する。AI がこの context を持っているだけで事故が減る。 -->

## AI 作業時の特記事項

<!-- TODO: このプロジェクト特有の AI 向け指示があれば。
例:
- "X モジュールを編集する前に必ず Y テストを実行する"
- "DB マイグレーションは絶対に手作業で行わない"
- "このリポジトリでは <ライブラリ名> の特定のバージョンを使うため、別バージョンの API を仮定しない"
-->
