---
id: WO-0000
slug: kebab-slug
status: draft
branch:
retry_limit: 3
budget_tokens: 200000
opened:
ordered:
accepted:
escalations: 0
rejections: 0
---

<!--
作業指示書 (Work Order) — AI エージェントが人間不在で走り切るための引き渡し契約。

これは「人が読む設計書」ではない。**自律稼働の事前条件**を全部そろえた契約である。
12 節すべてが埋まって初めて発注 (status: ordered) できる。埋まっていない節が 1 つでもあれば
`block-commit-if-wo-incomplete.sh` が commit を fail-closed で止める。

節を空にできない理由: 人間への質疑応答を減らすほど、AI は途中で新しい情報を受け取れない。
仕様に矛盾があれば AI は質問せず「もっともらしい解釈」を選ぶか停止する。だから自律稼働時間を
延ばす唯一の方法は、開始前に与える情報を増やすことになる。

**記入の規約**: `<...>` だけの行は placeholder = 未記入として機械判定される。
この HTML コメント内の説明文は判定対象外なので消さなくてよい。
該当なしの節も placeholder を消して「無し (理由)」と明示的に書くこと —
空欄は「全部こちらが決めた」と読まれ、AI を「空白を無音で埋めるしかない位置」に置く。

status の遷移:
  draft → pre-review → ordered → accepted
                          ↓
                       rejected (検収で差し戻し。frontmatter の rejections を +1 して
                                 ordered へ戻す。このカウンタが上流品質の metric になる)
-->

# WO-0000 — <この作業を 1 行で>

## 1. 目的

<!-- なぜこれを作るか。1 文。「何を実装するか」ではなく「どの欠落を埋めるか」を書く。 -->

<目的>

## 2. 作業範囲

<!--
実装者が触ってよい file の glob。1 行 1 glob。`order` skill がこれを `.bootstrap/lane` に
写すので、ここに書いていない file を触ると既存の lane gate が blocking する。
-->

- `<src/area/**>`

## 3. 変更禁止範囲

<!--
触ってはいけないもの。glob と、コードでない契約 (外部 API の形・DB schema・公開型) の両方。
「無し」なら「無し (理由)」と書く — 空欄にしない。
-->

- `<src/core/contract.ts>` — <理由>

## 4. 守るべき既存条件

<!--
既存設計のうち、この作業が壊してはいけない不変条件。charter.md / ADR / .bootstrap/arch への
**参照**で書く (転記でなく参照 — 正本が動けば指示も追従する)。
未決台帳の ID を参照する場合、その未決が OPEN のままだと発注できない。
-->

- <charter.md の該当セクション名 / ADR NNNN>

## 5. 優先順位

<!--
2 つの望ましさが衝突したとき、どちらを取るか。AI は衝突に当たると勝手に片方を選ぶので、
選び方を先に渡す。例: 後方互換 > 実行速度 > コードの短さ
-->

<優先順位>

## 6. 例外時の判断方法

<!--
仕様に書いていない状況・未定義の異常系に当たったときの既定の解釈方針。
「止まって聞く」は 10 節のエスカレーション条件に該当する場合だけ。
それ以外はここに書いた方針で AI が自分で進む。
-->

<例外時の方針>

## 7. 継ぎ目

<!--
この作業が繋ぐ、**既に実在する** API / 型 / 関数を名指しで列挙する。予測でなく Read/Grep で
確認して書く。ここが埋まっていると実装者は再探索しない (= 探索を発注側で終わらせる)。
存在しない名前を書くと `order` skill の実在検算で落ちる。
-->

- <Repository.save(id, value)> — <src/infra/repository.ts:42>

## 8. 完了条件 (DoD)

<!--
「動く」でなく「観測できる」形で書く。1 行 1 条件、`- [ ] D<N>: ` で始める。
行数は 9 節と一致していなければならない (検算)。
-->

- [ ] D1: <観測可能な条件>

## 9. 検証方法

<!--
8 節の各行に外部オラクル (AI 自身の主張でない証拠) を 1 つずつ与える。`- D<N>: ` で始める。
人間しか採点できない行は `HUMAN` と書く。行数は 8 節と一致すること。
-->

- D1: <テスト名 / read-back クエリ / 実測手順>

## 10. 停止条件

<!--
AI が「いつ止まるか」。ここが弱いとトークンを消費し続ける。3 つ全部書く。
再試行上限と予算上限の数値は frontmatter が正本 (retry_limit / budget_tokens)。
-->

- **再試行上限**: frontmatter `retry_limit` に従う (同一の失敗をこれを超えて再試行しない)
- **予算上限**: frontmatter `budget_tokens` を超えたら、進捗を記録して停止する
- **エスカレーション条件**: <何が起きたら人間を呼ぶか>

## 11. 決めてよいこと / 決めてはいけないこと

<!--
委譲した空白に名前を付ける。両方書く — 前身 2 プラグインが独立に到達した唯一の共通点であり、
実運用で効いていることが確認できている数少ない仕組み。
-->

**決めてよいこと** (可逆な裁量。聞かずに決めてよい):

- <UI の見た目 / 内部関数の分け方 / 変数名>

**決めてはいけないこと** (持ち帰り。触れたら 10 節のエスカレーション):

- <外部契約の形 / データモデル / 3 節の禁止範囲>

## 12. 事前レビュー

<!--
`pre-review` skill が仮想下流リーダーとして書く。全行が closed か deferred:<未決ID> に
なるまで発注できない。表に 1 行も無い状態 (= レビュー未実施) も発注できない。
-->

| # | 観点 | 指摘 | 状態 |
|---|---|---|---|
| 1 | <観点> | <指摘内容> | <closed または deferred:U1> |
