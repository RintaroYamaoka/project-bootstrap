# <project> — charter

<!--
このプロジェクトで **不可逆な判断だけ**を置く 1 ファイル。`charter` skill が保持する。

なぜ 1 ファイルか: 前身の 2 プラグイン (upstream-process / kanban-flow) は上流の成果物を
13 種類のファイルに割り、どちらも「重くて使われない」で死んだ。書き分けの規則そのものが
記憶の負担になり、負担が使用を止めた。ここでは**永続ファイルは charter.md 1 つだけ**にする。

置くもの: 目的 / 不変のコア / 制約 / 未決台帳。
置かないもの:
  - 可逆な判断 → 書かない (コードとテストが正本)
  - 不可逆な**アーキ**判断 → `docs/decisions/` の ADR (状態遷移と撤回が要るため別機構)
  - 個別作業の指示 → WO (`wo/<id>-<slug>.md`)
  - 進捗・状態 → 持たない (git log と WO の status が持つ)

更新は追記が既定。書き換えるときは、その行が WO の 4 節から参照されていないか先に確認する
(前提を黙って抜くと、実装者の成果物が実装者の落ち度の顔をして壊れる)。
-->

## 目的

<何を解決するか。1-3 文。北極星と当面の期待値は分けて書く>

## 不変のコア

<絶対に変わらない芯。ここに書いたものは WO の 3 節 (変更禁止範囲) の根拠になる。
 「変わらない」と言い切れないものはここに書かず、下の未決台帳へ>

## 制約

<日付を持たない、プロジェクト全体を通して常に真であるべき前提。
 決定ログの 1 行が十分 evergreen だと分かったらここへ昇格させてよい>

- <例: 外部 API のレート制限は 60 req/min。設計はこれを前提にする>

## 決定ログ

<可逆・不可逆を問わず「決めた」ことの 1 行記録。理由を必ず添える —
 理由の無い決定は、後から AI にも人間にも再提案される>

| 日付 | 決めたこと | 理由 |
|---|---|---|
| <YYYY-MM-DD> | <決定> | <理由> |

## 未決台帳

<まだ決めていないこと。**無音で埋まってはならない**唯一の不変条件がここに宿る。
 WO の 4 節から OPEN な未決を参照したまま発注することはできない
 (`block-commit-if-wo-incomplete.sh` が止める)。
 状態は OPEN / CLOSED のどちらか。CLOSED にするときは決定ログにも 1 行足す>

| ID | 論点 | 状態 |
|---|---|---|
| U1 | <決まっていないこと> | OPEN |
