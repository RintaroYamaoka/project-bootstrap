# verification plan — detector のすり抜け穴の全塞ぎ (ADR 0019) + JSON field 抽出の単一権威化 + lib テスト補完
# 由来: 2026-07-10 全体監査。穴はすべて実コマンドで再現してから (red) 直した (green)。
# 落とした範囲: 実 Claude Code harness が渡す本物 payload での再検証 (合成 JSON で pin。README の harness contract 表が残余を担う) / quote・escape 系の tokenizer 限界 (quoted separator = over-detect 方向、quoted git-head/subcommand token・`sh -c` 内 = under-detect し得る。full shell parser なしには既約、git-invocation.sh header に明示)
# STATUS | kind | behaviour | oracle | by | evidence/note
PASS | e2e         | 修正前の再現: git -C /repo push origin main / git -c k=v push / git --git-dir=.git push / git -P push が cmd_has_git_push に MISSED、destination 0 件。commit 側は /usr/bin/git commit / ./git commit / git -C /r commit / git -c k=v commit が regex に MISSED | 実 lib を source した実行結果 (推測でなく再現から着手) | ai | 本 session の実測ログ (protected-branch.sh 旧版 + 旧 regex に対して全ケース MISSED を確認してから実装)
PASS | unit        | git グローバルオプション (-C/-c/--git-dir=/--git-dir 空白/-P/--no-pager 連鎖) と path-prefixed git (/usr/bin, ./) を跨いで subcommand が検出される | 期待値 | ai | tests/hooks/git-invocation.test.bash 26 assert (検出 11 + 否定 7 + arglines 8)
PASS | unit        | 誤検出しない: mygit/legit、-C の値が subcommand に読まれない、echo push が arm しない | 期待値 | ai | git-invocation.test.bash 否定ケース群
PASS | e2e         | 保護 push gate: git -C <path> push origin main / git -c k=v push / /usr/bin/git push が exit 2 で block | 実 temp repo + 実 hook の exit code | ai | block-push-to-protected.test.bash 3 新ケース
PASS | e2e         | git push --all origin を feature branch 上から打つと、local に protected branch (main) が在る限り exit 2。--mirror も同様。protected な local branch が無ければ素通し (over-block しない) | 実 temp repo + exit code | ai | block-push-to-protected.test.bash 3 新ケース (--all block / --mirror block / release/* のみ保護で pass)
PASS | e2e         | freshness gate (block-stale-write): --all push が stale trunk を含むとき block、git -C 形も検出 | 実 local-remote pair + exit code | ai | block-stale-write-to-protected.test.bash 2 新ケース
PASS | unit        | push_pushes_all_branches: --all/--branches/--mirror を全 segment で検出、-o の値の --all は誤検出しない、非 push command の --all は数えない | 期待値 | ai | protected-branch.test.bash 8 新 assert
PASS | e2e         | commit 系 gate 5 本 (test/lint/arch/lane/cross-wip) が /usr/bin/git commit と git -C/-c 形を検出して各自の判定を実行する (失敗する fixture が exit 2 になることで検出を証明) | 実 fixture + exit code | ai | 各 test file に 2-3 新ケース (計 11 assert)。修正前に全て exit 0 (red) を確認
PASS | unit        | merge gate detector: git -C /repo merge feat/x が検出され target が列挙される、-C の値は target に読まれない | 期待値 | ai | merge-targets.test.bash 5 新ケース
PASS | e2e         | block-add-all: bare stash が compound command のどの segment に居ても block (`git stash && echo done` / `git stash && git stash push -m msg -- src/a`)、/usr/bin/git add -A / git -C /x add -A も block。targeted stash + stash pop の組は素通し | 実 hook exit code | ai | block-add-all.test.bash 8 新ケース + 既存 61 assert 全維持 (characterization パリティ)
PASS | unit        | parse_json_string_field: `,` `}` を含む path が途中切りされない、escape が decode される、key 不在は rc 1、最初の出現が勝つ、parse_command は alias | 期待値 | ai | parse-command.test.bash 6 新ケース
PASS | e2e         | 旧 grep 抽出の fail-open 再現→修正: `src/foo,bar/baz.ts` (comma 入り path) の companion なし編集が block される (旧実装は `src/foo` に切れて素通し = red で確認) | 実 hook exit code | ai | require-test-companion.test.bash 新ケース。8 call site (file_path×5 / transcript_path / cwd×2) を parse_json_string_field に移行
PASS | unit        | lane-set: 活性 board の branch ∪ linked worktree の branch、全 done board は寄与しない (活性≠存在)、main worktree 自身の branch は lane でない | 実 temp repo + board fixture | ai | tests/hooks/lane-set.test.bash 9 assert。両 merge gate の既存スイート (28+29 assert) が回帰オラクル
PASS | unit        | lib テスト補完: commit-files (-a/-am/--all の sweep、削除 file 包含、-m の値で誤 sweep しない) / lane-match (glob 照合 + 別 worktree 判定) / detect-test-suite (runner-on-PATH 必須、stub PATH で決定論化) / source-face (source/test/config 分類) | 期待値 | ai | 4 新 test file (9+10+8+17 assert)
PASS | unit        | 新テストが実バグを 1 件検出→修正: source-face が repo-root の tests/** を source 面と誤分類 (*/tests/* に root anchor が無い) → over-block 方向の穴を塞いだ | 期待値 (red→green) | ai | source-face.test.bash "a file under tests/" が red → tests/* 等の anchor 追加で green
PASS | e2e         | inject-action-memory の entry 挙動: 非 Bash / 非 action / preview deploy / parse 不能は無音 exit 0 (never-block)、prod-deploy と data-backfill は unarmed repo でも default memo を additionalContext で emit、armed key は repo memo を追記 | 実 hook の stdout/exit code | ai | tests/hooks/inject-action-memory.test.bash 23 assert (唯一 entry テストの無かった hook)
PASS | mutation    | 主要 assert の実効性 (緑の嘘の排除): 実装を壊すと赤くなるか 6 変異で確認 | 注入した変異をテストが殺すか | ai | 全 kill: ①global-option skip 無効化 (10 赤) ②push destination emit 除去 (26 赤) ③push_pushes_all_branches 常時 1 = --all fail-closed 無効化 (8 赤) ④parse_json_string_field に ',' truncation 再導入 (3 赤) ⑤bare-stash block 無効化 (10 赤) ⑥lane-set から worktree branch 脱落 (6 赤)。各変異後 git restore で復元し全 suite 緑を再確認
PASS | contract    | 全 hook スイート回帰なし + shellcheck | tests/hooks/run.sh / shellcheck -S warning | ai | 45 suites, 0 failed (38→45 に増加)。shellcheck -S warning hooks/*.sh hooks/lib/*.sh ゼロ warning (既存残余 2 件も解消)
PASS | contract    | 配備面: hook の新規追加なし (hooks.json 不変)、doctor.sh の vendored REQ は hook 単位のリストで lib を列挙しないため更新不要 (新 lib は既存 hook の source 先)、hooks/README.md の台帳は更新済み | doctor.sh / hooks.json の実査 | ai | doctor.test.bash 含む全 suite 緑。README に git-invocation / lane-set / parse_json_string_field / --all fail-closed を記載
PASS | human       | **gate 約束の変更**: `git push --all/--branches/--mirror` を「全 local branch が destination」として fail-closed 化 (旧: current-branch 判定に落ちて実質 fail-open)。意図的に全 branch を push したい場合も protected を含む限り block される | 設計者 (単一 orchestrator) の意図 | human | 2026-07-10 設計者が全体監査の修正方針 (--all の全branch扱い含む) を提示済みの上で「全評価★5化を計画・実装・リリースせよ」と明示指示。ADR 0019 に trade-off (bypass = /permissions 一時 deny) を明記
DROP | e2e         | 実 Claude Code harness の本物 payload での再検証 | n/a | ai | 合成 JSON で期待スキーマを pin する方針は既存全テストと同一。実 harness の key rename 検出は README「Claude Code 契約への依存」表 + アップグレード時の手動再検証が担う設計 (cry-wolf 回避のため runtime 自動判定はしない — 既存決定)
DROP | unit        | quote・escape 系の tokenizer 限界: quoted 引数内の separator / `git <sub>` 列 (over-detect 方向) と、git-head・subcommand token 自体への quote/escape・glued redirection・`sh -c` 内 (`git 'push'` / `"git" push` / `\git push` / `git pu\sh` / `git push>/dev/null` / `sh -c "git push …"` = silent under-detect、実測確認) | n/a | ai | full shell parser なしには既約 (旧 regex 時代からの同一穴 = 退行ではない)。under-detect 側の net は commit 系 gate + server 側 (branch protection + required check、ADR 0012)。git-invocation.sh header に両方向を明示済み
