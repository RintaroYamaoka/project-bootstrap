# verification plan — skills/README の重複・肥大の解消 + advisory の正直化 (規範消失ゼロ)
# 由来: 2026-07-10 設計者の全体監査。docs-only lane (runtime surface 無し) — オラクルは grep/実測による機械検査 + 監査指示との照合。
# 落とした範囲: frontmatter description の圧縮 (task 5) は「自信が無ければ触らない」条項に従い不実施 / hooks/README.md 等 lane scope 外の doc は不変更
# STATUS | kind | behaviour | oracle | by | evidence/note
PASS | manual      | project-bootstrap SKILL.md が 30% 以上減量されている | wc -c/-l の実測値 | ai | 41322B/372 行 → 28916B/225 行 = **-30.02% bytes / -39.5% lines** (frontmatter description 1522B は不変のまま達成)
PASS | manual      | verification SKILL.md の 6th/7th seam 再説明が 1 権威 + ポインタに畳まれている | 変更後の grep (再説明箇所が節ポインタになっている) | ai | 権威 = §「6 番目の seam」「7 番目の seam」blockquote。Step 2 bullet / Step 3 表 oracle 列 / Step 4 gameable 段落は「7 番目の seam 節」参照に。30015B → 28708B (行数 134 不変)。落ちかけた引用 METR は権威節へ集約
PASS | manual      | README 提供物表が 1 行要約 + 正本ポインタに畳まれている | 変更後の README 実読 | ai | skills / ADR / hooks+lib / scripts+templates の 4 表に再構成、冒頭に「規範の権威は各ファイル本文」と明記。README 全体 38851B/216 行 → 25226B/233 行 = **-35.1% bytes** (行数は表分割で微増)
PASS | contract    | 規範消失ゼロ: 変更前に skills+README が参照していた hook 名 / lib 名 / script 名 / ADR 番号 / incident dir 名の全 54 参照が、変更後も repo 内の権威箇所に最低 1 回残る | 機械検査 (baseline を変更前に抽出し変更後に word-boundary regex で照合) | ai | baseline = hooks 18 + lib 14 + scripts 4 + ADR 15 + incident 3 = 54 参照 (scratchpad/refs-before.json)。変更後 MISSING = 0。唯一 skills+README から外れかけた lib:source-face は README doctor 行に復元し skills+README 内にも残存
PASS | contract    | ポインタ先の実在: 編集した 8 file 中の相対 path 参照 (`skills/` `hooks/` `scripts/` `templates/` `docs/` `tests/`) がすべて実在する | 機械検査 (os.path.exists、78 path) | ai | 実在しない 3 件はいずれも opt-in/runtime 生成物 (docs/sprint/board.json / docs/sprint/.gate / docs/verification/contracts) で、採用 repo でのみ実体化する仕様どおり — broken pointer ではない
PASS | contract    | 権威の一方向化: 畳んだ規範の各々に単一権威が指名され、旧所在はポインタであり、**ポインタ先に規範が実在する** | 変更後 diff の実読 + adversarial re-review | ai | 並列判定・wip_limit・.gate 記録形式 = sprint-plan skill §大前提 / intent・オラクル doctrine・継ぎ目の型 = verification skill / verification 4 罠 = project-bootstrap (integrate Step 4・README が既にここを指すため維持、async 罠のみ verification skill へ委譲) / marker 後方互換 = README opt-in 節注記 + project-bootstrap ADR 0015 注記の各 1 箇所。**初回 push はポインタ先不在の規範消失 1 件あり (reject)**: 「Class A/B は成果物上同じ表層形 → 分類を試みず両方に効く緩和を張る」を Step 2/Step 4 から削った際、権威指名した 7th seam blockquote に存在しなかった → 同 blockquote に復元済み (grep「表層形」1 hit)。教訓: 消失検査は名前 token だけでなく規範文にも要る — 名前 grep は prose 規範を素通しした
PASS | manual      | marker 表記が新フォルダ形 (`.bootstrap/<name>`) に統一されている | grep '\.bootstrap-' の残存確認 | ai | 実残存 (literal `.bootstrap-`) は 3 箇所のみ: README:73 (後方互換の唯一注記) / skills/project-bootstrap/SKILL.md:172 (ADR 0015 注記) / skills/sprint-plan/SKILL.md:3 (frontmatter description — auto-fire トリガーのため不変更 = 落とした範囲)。本文の旧 flat 主表記はゼロ (sprint-plan lane 行の worktree-root 補足は文言のみで literal token を含まない)
PASS | manual      | plan skill §鉄則に「hook 強制でなく advisory」の正直な注記があり、downstream 裏付け (block-unplanned-feature-build / verification gate) を指す | 変更後の実読 + 監査指摘文言との照合 | ai | skills/plan/SKILL.md §鉄則直下に blockquote 追加。意味 (Edit/Write 禁止) は不変更
PASS | manual      | integrate Step 3 が「hook の強制は pre-merge スイートまで / post-merge 結合スイートは advisory / backstop はサーバ側 CI required check」と書き分けている | 変更後の実読 + 監査指摘文言との照合 | ai | skills/integrate/SKILL.md Step 3-2 に「正直な書き分け」追記 (ADR 0012 参照)。「これが verification の本体」という保証に読める文言を除去
PASS | contract    | README の hook 数が実体と一致 (lane C 引き継ぎ) | hooks/hooks.json の登録実体 (前 lane の検証で 20 と実測済み) | ai | README hooks/hooks.json 行を 19 → 20 hook に修正、lane 強制も「編集 / commit を blocking (ADR 0017)」に更新。ADR 0008 行の「default 19 hook 外」は当時の引用のため据え置き (lane C 判断を踏襲)
PASS | contract    | 全 hook スイート回帰なし (docs-only だが commit gate が要求) | tests/hooks/run.sh | ai | 38 suites, 0 failed (実行ログは最終 commit 前に取得)
DROP | manual      | frontmatter description (project-bootstrap / verification / sprint-plan) の圧縮 | n/a | ai | auto-fire トリガー語彙・発火条件を兼ねるため「自信が無ければ触らない」指示に従い不実施。sprint-plan description 内の旧 flat 表記 `.bootstrap-lane` も同理由で据え置き (本文は統一済み)
DROP | e2e         | 減量後 skill の実 session ロード挙動 (auto-fire が変わらないこと) の live 確認 | n/a | ai | description (トリガー面) を一切変えていないため発火条件は構造的に不変。本文はロード後に読まれる参照情報で、ロード判定に関与しない
